from setuptools import setup, find_packages

setup(
    name='pipeline_datalake',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here if needed
        # Or leave it to the requirements.txt
    ],
)
